import { PixelRatio, StyleSheet } from "react-native";

export const AuthScreenContainerStyle = StyleSheet.create({
    AuthScreenContainerContainer:{
        flex:1,
        padding:16/PixelRatio.get()   
    }
})